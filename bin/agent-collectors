#!/bin/sh
# Launcher for the engine; keeps the documented CLI path stable.
# The real module lives in agent_collectors.py (importable for tests/typing).
exec "$(dirname "$0")/agent_collectors.py" "$@"
